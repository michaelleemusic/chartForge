RewriteEngine On

# Don't rewrite existing files or directories
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Route API requests through PHP
RewriteRule ^api/(.*)$ demo/index.php [L,QSA]

# Route root to demo
RewriteRule ^$ demo/index.html [L]
